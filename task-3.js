function make_avg(numbers, size){
    let sum = 0;
    for(const number of numbers){
        sum += number;  //age plus trpor equal
    }
    return sum / size;
}
const result = [10, 26, 27, 15,3];
console.log(make_avg(result, result.length)); //upore joyta perameter ase tototae dite hbe ekhane budddhu
